Installing Memcached In Debian Linux

```angular2html
apt-get install memcached
```

Once installed, memcached can simply be below command:

```angular2html
$ memcached
```

Install Python Packages Using requirements.txt

```angular2html
pip install -r requirements.txt
```
